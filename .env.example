# Supabase Configuration
# Get these values from your Supabase project dashboard:
# https://app.supabase.com → Your Project → Settings → API

# Project URL (Settings → API → Project URL)
VITE_SUPABASE_URL=your_supabase_project_url_here

# Anonymous/Public Key (Settings → API → Project API keys → anon/public)
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Access Token for Supabase CLI (Settings → Access Tokens → Generate new token)
# Only needed for database migrations and CLI operations
SUPABASE_ACCESS_TOKEN=your_supabase_access_token_here
